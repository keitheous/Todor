= form_with(model: todo, local: true) do |form|
  - if todo.errors.any?
    #error_explanation
      %h2
        = pluralize(todo.errors.count, "error")
        prohibited this todo from being saved:
      %ul
        - todo.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :task
    = form.text_field :task, id: :todo_task
  .field
    = form.label :due_date
    = form.datetime_select :due_date, id: :todo_due_date
  .field
    = form.label :participant
    = form.text_field :participant, id: :todo_participant
  .actions
    = form.submit
